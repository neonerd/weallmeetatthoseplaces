export interface ValueGeneratorWeightedOption {
    value: any
    probability: number
}

/**
 * Randomly generates a value from options, taking into account their probability
 * @param options 
 */
export function generateValueWithWeightedProbability (options: ValueGeneratorWeightedOption[]) {
}

/**
 * Randomly generates a value form options, with equal probability
 * @param options
 */
export function generateValueRandomly (options: any[]) {

}